# Automatically Generated by Vagrant Config Generator, see https://github.com/jianan1104/vagrantfile-generator
Vagrant.configure('2') do |config|
  (1..1).each do |i|
  config.vm.define "desafio11" do |machine|
    machine.vm.box = 'ubuntu/focal64'
    machine.vm.network "private_network", ip: '192.168.100.50'
    machine.vm.hostname = "desafio11"
    machine.vm.provider "virtualbox" do |vb|
      vb.name = "desafio11-#{i}"
      vb.cpus = '2'
      vb.memory = '2048'
    end
  end
  end
end
